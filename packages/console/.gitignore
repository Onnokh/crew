# Generated by the TanStack Router Vite plugin from src/routes/ on dev/build —
# never hand-edited, so it stays out of version control (regenerated locally and
# in the Docker build stage).
src/routeTree.gen.ts
