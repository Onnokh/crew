# Changelog

All notable changes to the Crew OpenCode plugin are documented here.

## 0.1.0

- Add the OpenCode plugin package with a `.opencode/plugins/crew.js` plugin file.
- Add OpenCode skill packaging for the same `crew`, `ask-crew`, `reflect`, and
  `introduce` workflows used by the Claude, Codex, and Cursor plugins.
